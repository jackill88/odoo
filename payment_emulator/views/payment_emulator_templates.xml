<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <template id="inline_form">
        <div t-attf-id="emulator-container-{{provider_id}}" class="payment_emulator_inline">
            <p class="text-muted mb-0">
                Click <strong>Pay</strong> to complete the payment (emulated success).
            </p>
        </div>
    </template>

</odoo>
